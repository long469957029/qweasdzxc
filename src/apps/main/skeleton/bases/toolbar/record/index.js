import './index.scss'

const RecordView = Base.ItemView.extend({

  template: require('./index.html'),

  events: {
  },

  initialize() {
  },

  serializeData() {
  },

  onRender() {
  },
})

module.exports = RecordView
