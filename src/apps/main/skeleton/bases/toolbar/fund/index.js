import './index.scss'

const FundView = Base.ItemView.extend({

  template: require('./index.html'),

  events: {
  },

  initialize() {
  },

  serializeData() {
  },

  onRender() {
  },
})

export default FundView
