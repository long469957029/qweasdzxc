import './index.scss'

const FeedbackView = Base.ItemView.extend({

  template: require('./index.html'),

  events: {
  },

  initialize() {
  },

  serializeData() {
  },

  onRender() {
  },
})

module.exports = FeedbackView
